[SYSTEM]
You are a senior software validation engineer and security auditor. Your task is to perform "Requirement Reverse Engineering" on a paired set of C/C++ code snippets.
You have been provided with strategically sliced "Vulnerable Code" (Bad Code) and "Fixed Code" (Good Code), along with the developer's commit message and the official CVE description.

[USER]
<CVE_DESCRIPTION>
{cve_desc}
</CVE_DESCRIPTION>

<COMMIT_MESSAGE>
{commit_message}
</COMMIT_MESSAGE>

<SLICED_BAD_CODE>
{sliced_bad_code}
</SLICED_BAD_CODE>

<SLICED_GOOD_CODE>
{sliced_good_code}
</SLICED_GOOD_CODE>

[INSTRUCTION]
Your goal is to perfectly reverse-engineer the unstated security requirement (the "Contract") that the Good Code satisfies and the Bad Code violates.
Define this requirement formally using BDD-style Gherkin syntax (`.feature` format).

Follow these analytical steps:
1. **Analyze the Sliced Diff**: The code provided is a distilled slice showing exactly where the vulnerability and fix occur. What formal rule is the Good Code enforcing that the Bad Code misses?
2. **Consult the Oracle**: Factor in the `CVE_DESCRIPTION` and `COMMIT_MESSAGE`. **CRITICAL PRIORITY RULE:** If the text description contradicts or mismatches the actual code diff, ALWAYS trust the code diff as the absolute ground truth.
3. **Draft the Feature**: Write a single, highly cohesive Gherkin `Feature` that describes the security capability (e.g., `Feature: Strict integer overflow prevention during buffer allocation`).
4. **Draft the Scenarios**: Write one or more Gherkin `Scenario` (or `Scenario Outline`) blocks that define the expected, secure behavior of the system.
    - **Be Focused & Minimal:** Only include scenarios strictly related to the vulnerability and its fix.
    - **Do NOT be Verbose:** Keep the Feature and Scenario descriptions incredibly concise. Do not write lengthy explanations within the Gherkin steps. Focus purely on the core logic.
    - **Be EXACT with Constraints:** Do not write vague or high-level summaries (e.g. "bounds should be checked" or "ensure proper error handling"). You MUST mathematically or programmatically specify exactly WHAT the secure boundary condition, null-check, or type verification must be (e.g. "When size + 1 overflows to 0"). If you write a vague requirement, you will fail the audit!
    - **Act as a Benchmark:** These scenarios act as a definitive security contract. The Good Code will pass them, and the Bad Code will fail them.
    - **Describe Desired Behavior:** **DO NOT** write a Scenario describing how the Bad Code crashes or gets exploited. A feature file dictates *desired* and *secure* behavior only.

Output your response STRICTLY in the following XML format. Do not output any markdown outside of these tags.

<THINKING>
(EXTREMELY CONCISE analysis of the contract. MAXIMUM 4 SENTENCES. Do not write a novel. Focus on exactly what Gherkin scenarios are needed)
</THINKING>
<GHERKIN_FEATURE>
(The formal Gherkin .feature document)
</GHERKIN_FEATURE>
