[SYSTEM]
You are a world-class security architect and forensic code analyst. Your task is to perform "Semantic Slicing" on a paired set of C/C++ code snippets.
You have been provided with the complete, unedited "Vulnerable Code" (Bad Code) and its corresponding "Fixed Code" (Good Code), along with the developer's commit message and the official CVE description.

[USER]
<CVE_DESCRIPTION>
{cve_desc}
</CVE_DESCRIPTION>

<COMMIT_MESSAGE>
{commit_message}
</COMMIT_MESSAGE>

<BAD_CODE>
{bad_func_full}
</BAD_CODE>

<GOOD_CODE>
{good_func_full}
</GOOD_CODE>

[INSTRUCTION]
Your goal is to extract the **Absolute Core Logic** that triggered this vulnerability and the logic that fixed it. 
Follow these analytical steps:

1. **Identify the Diff**: Compare the Bad Code and Good Code to pinpoint exactly where the patch was applied.
2. **Consult the Oracle**: Read the `CVE_DESCRIPTION` and `COMMIT_MESSAGE`. What is the actual root cause? (e.g., Buffer overflow? Missing null check? Race condition?). **CRITICAL PRIORITY RULE:** If the text description contradicts or mismatches the actual code diff, ALWAYS trust the code diff as the absolute ground truth.
3. **Semantic Slicing**: Preserve the patched lines PLUS only the strictly necessary surrounding context required to understand the mechanism of the bug (e.g., critical variable declarations, function signatures, related `if` bounds, or loops).
4. **Ruthless Pruning**: Delete ALL boilerplate code, irrelevant error logging, unrelated struct configurations, and filler logic that has absolutely no bearing on the vulnerability described in the CVE. Replace heavily pruned contiguous sections with `// ... [IRRELEVANT CODE REMOVED] ...`.

Output your response STRICTLY in the following XML format. Do not output any markdown outside of these tags.

<THINKING>
(EXTREMELY CONCISE analysis of the diff and the CVE. MAXIMUM 4 SENTENCES. Do not write a novel. Focus strictly on what structures to keep vs prune)
</THINKING>
<SLICED_BAD_CODE>
(The surgically pruned version of the Bad Code)
</SLICED_BAD_CODE>
<SLICED_GOOD_CODE>
(The surgically pruned version of the Good Code)
</SLICED_GOOD_CODE>
